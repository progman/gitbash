Git for bash  v0.2.6

Install this

	1) git clone git://github.com/progman/gitbash.git ~/.config/gitbash
	2) echo "source ~/.config/gitbash/gitbash;" >> ~/.bashrc
	3) Use this (cd in something git repo)

