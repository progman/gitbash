Git for bash  v0.2.8

Install this

	$ mkdir -m 0700 ~/.config &> /dev/null;
	$ git clone https://github.com/progman/gitbash.git ~/.config/gitbash;
	$ echo "source ~/.config/gitbash/gitbash;" >> ~/.bashrc;

Use this
	Run gitbash_help or just enter into any git repository
